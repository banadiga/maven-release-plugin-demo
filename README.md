# maven-release-plugin-demo
Maven Release plugin demo

- Prepare release

```bash
mvn release:prepare
```

- Perform release

```bash
mvn release:perform
```

- Clean up after prepare

```bash
mvn release:clean
```
